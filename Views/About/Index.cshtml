@using TravelTripProje.Models.Siniflar
@model List<Hakkımızda>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_TestLayout.cshtml";
}

<div class="content">
    <div class="wrap">
        <div class="about">
            <h3>Hakkımızda</h3>
            @foreach (var x in Model)
            {
                <img src="@x.FotoUrl" style="width:500px;height:450px;" alt="" />

                <p>@x.Aciklama</p>
            }
        </div>
    </div>
</div>

